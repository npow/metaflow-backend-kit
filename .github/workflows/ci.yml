name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.10", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Validate scaffold output
        run: |
          metaflow-backend-scaffold _citest
          metaflow-backend-validate _citest/
          rm -rf _citest/

      - name: Run tests
        # Exit code 5 = no tests collected (compliance tests require a live backend).
        # Treat that as a pass; any other non-zero exit is a real failure.
        run: pytest -v --ignore=metaflow_backend_kit/compliance || { ec=$?; [ $ec -eq 5 ] && exit 0 || exit $ec; }
